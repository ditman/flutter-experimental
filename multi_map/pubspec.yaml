name: multi_map
description: "A multi-view example app for the web. Now with maps."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.2.0

dependencies:
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3
  pointer_interceptor: ^0.10.1
  web: '>=0.3.0 <0.5.0'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
